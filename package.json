{
  "name": "scandalous",
  "private": true,
  "description": "Lint/test task runner for scandalous (JS via npm scripts, Ruby via bundler)",
  "scripts": {
    "test": "vitest run && bundle exec rspec -fd spec",
    "lint-js": "standard",
    "test-js": "vitest run",
    "lint-rb": "bundle exec standardrb",
    "test-rb": "bundle exec rspec -fd spec",
    "lint-html": "html-validate --ext=erb views/",
    "check": "npm run lint-js && vitest run --reporter=dot && npm run lint-rb && bundle exec rspec spec && npm run lint-html"
  },
  "devDependencies": {
    "html-validate": "^11.5.6",
    "sinon": "^19.0.2",
    "standard": "^17.1.2",
    "vitest": "^4.1.10"
  },
  "standard": {
    "env": [
      "browser"
    ],
    "globals": [
      "context"
    ]
  }
}
