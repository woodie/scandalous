{
  "name": "scandalous",
  "private": true,
  "description": "Lint/test task runner for scandalous (JS via npm scripts, Ruby via bundler)",
  "scripts": {
    "test": "vitest run && bundle exec rutidy -fv spec",
    "lint-js": "standard --fix",
    "test-js": "vitest run",
    "lint-rb": "bundle exec standardrb --fix",
    "test-rb": "bundle exec rutidy -fv spec",
    "lint-html": "html-validate --ext=erb views/",
    "check": "standard && vitest run --reporter=dot && bundle exec standardrb && bundle exec rspec spec && npm run lint-html"
  },
  "devDependencies": {
    "html-validate": "^11.5.6",
    "sinon": "^19.0.2",
    "standard": "^17.1.2",
    "vitest": "^4.1.10"
  },
  "standard": {
    "env": [
      "browser"
    ],
    "globals": [
      "context"
    ]
  }
}
