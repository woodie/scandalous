{
  "name": "scandalous",
  "private": true,
  "description": "Lint/test task runner for scandalous (JS via npm scripts, Ruby via bundler)",
  "scripts": {
    "lint-js": "standard",
    "test-js": "vitest run",
    "lint-rb": "bundle exec standardrb",
    "test-rb": "bundle exec rspec -fd spec",
    "check": "npm run lint-js && vitest run --reporter=dot && npm run lint-rb && bundle exec rspec spec"
  },
  "devDependencies": {
    "sinon": "^19.0.2",
    "standard": "^17.1.2",
    "vitest": "^2.1.9"
  },
  "standard": {
    "env": [
      "browser"
    ],
    "globals": [
      "context"
    ]
  }
}
